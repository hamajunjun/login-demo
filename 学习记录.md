# 学习记录

## 项目基本信息

- 项目名称：login-demo
- 技术栈：Spring Boot + MyBatis + MySQL + JWT
- 项目路径：`login-demo/`
- JDK 版本：17
- Spring Boot 版本：2.6.13

## 已掌握的内容

### 1. 项目结构理解

| 包名 | 作用 |
|------|------|
| `entity` | 数据库表对应的 Java 类 |
| `mapper` | 写 SQL 的地方 |
| `service` | 业务逻辑层 |
| `controller` | 接收前端请求、返回结果 |
| `util/config/common` | 工具类、配置、统一返回格式 |

### 2. 前端 HTML 调用后端的两种方式

- **传统表单提交**：`register.html` 使用 `<form action="/user/register" method="post">`
- **fetch 请求**：`login.html`、`post.html`、`community.html` 使用 JavaScript 的 `fetch`

### 3. JWT 登录认证流程

1. 登录成功后，后端返回 token
2. 前端把 token 存到 `localStorage`
3. 后续请求需要登录的接口时，在请求头里带上 `Authorization: token`
4. 后端 `JwtInterceptor` 验证 token

## 今天新增/学习的后端功能

### 1. 删除用户

- `UserMapper`：`@Delete("DELETE FROM user WHERE id=#{id}")`
- `UserService` / `UserServiceImpl`：`deleteUserById(Long id)`
- `UserController`：`DELETE /user/{id}`

### 2. 根据用户名模糊查询用户

- `UserMapper`：`@Select("SELECT * FROM user WHERE username LIKE CONCAT('%',#{username},'%')")`
- `UserService` / `UserServiceImpl`：`searchUsersByUsername(String username)`
- `UserController`：`GET /user/search?username=xxx`

### 3. 更新用户信息（根据 ID）

- `UserMapper`：`@Update("UPDATE user SET username=#{username},email=#{email} WHERE ID=#{id}")`
- `UserService` / `UserServiceImpl`：`updateUserInfo(Long id, String username, String email)`
- `UserController`：`POST /user/update?id=xxx&username=xxx&email=xxx`
- 注意：这是管理端功能，因为可以传任意 id 改任意用户

### 4. 当前登录用户修改自己的信息

- `UserService` / `UserServiceImpl`：`updateCurrentUserInfo(String currentUsername, String newUsername, String newEmail)`
- `UserController`：`POST /user/updateMyInfo?username=xxx&email=xxx`
- 从 token 里取当前用户名，只能改自己的信息

## 需要注意的问题

1. `UserMapper.java` 里 `updatePassword` 方法的 SQL 有语法错误：`#{username` 缺少右括号 `}`，需要修复。
2. `UserServiceImpl` 中的方法如果实现了接口方法，建议加上 `@Override` 注解。
3. `UserService` 接口中需要声明所有 `UserServiceImpl` 要实现的方法，否则 IDEA 会标红。

## 已理解的概念

- Controller → Service → Mapper → 数据库 三层架构
- `@RequestParam`、`@PathVariable`、`@RequestHeader` 的区别
- `@Param` 注解的作用
- `fetch` 请求的基本用法
- `localStorage` 存取 token
- JWT token 的传递和验证
- SQL 的 `LIKE` 模糊查询
- MyBatis 注解：`@Select`、`@Insert`、`@Update`、`@Delete`

## 下一步可选方向

### 用户端功能
- 用户删除自己的账号
- 用户查看自己发的帖子
- 用户退出登录（前端清 token）
- 用户修改密码（已有，但可复习）

### 管理端功能
- 启用/禁用用户（需要加 `status` 字段）
- 管理员查看所有用户
- 管理员删除任意用户

### 项目优化
- 修复 `updatePassword` 的 SQL 语法错误
- 给 `UserServiceImpl` 的方法统一加 `@Override`
- 添加 `findById` 方法统一查询风格
- 学习全局异常处理 `GlobalExceptionHandler`

## 2026-08-13 学习记录

### 修复与优化
1. 修复 `UserMapper.updatePassword` SQL 语法错误（补全 `}`）
2. 给 `UserServiceImpl.deleteUserById` 添加 `@Override` 注解
3. 修复 `UserController` 中 `delete`、`search`、`update`、`updateMyInfo` 缺失或错误的映射注解

### 新增后端功能
1. 根据 ID 查询用户
   - `UserMapper.findById`
   - `UserService.findById`
   - `UserController.GET /user/{id}`

2. 当前登录用户查看自己发的帖子
   - `PostMapper.findByUserId`
   - `PostService.listByUserId`
   - `PostController.GET /post/myList`

3. 当前登录用户删除自己的账号
   - `UserService.deleteCurrentUser`
   - `UserController.POST /user/deleteMyAccount`

### 新增前端功能
1. `post.html` / `community.html`：添加退出登录按钮
2. 新建 `my-posts.html`：展示当前登录用户的帖子列表
3. `my-posts.html`：添加注销账号按钮和确认逻辑
4. `post.html` / `community.html`：添加“我的帖子”导航链接

### 新学知识
1. 全局异常处理 `GlobalExceptionHandler`
   - `@RestControllerAdvice`：全局捕获 Controller 异常
   - `@ExceptionHandler`：指定处理某种异常
   - `@Slf4j`：Lombok 日志注解
2. `PageHelper` 分页原理
   - `PageHelper.startPage(pageNum, pageSize)` 自动加 `LIMIT`
   - `PageInfo<>(list)` 包装分页结果
3. HTTP 请求基础
   - `Content-Type` 的作用
   - 表单提交与 JSON 提交的区别

### 进行中：管理员功能
1. 用户表添加 `role` 字段（`DEFAULT 'USER'`）
2. 将 `admin` 用户 role 设为 `ADMIN`
3. `User` 实体类添加 `role` 属性
4. `JwtUtil` 在 token 中携带 `role`，并添加 `getRole()` 方法
5. 注册新用户时默认 `role = "USER"`
6. `UserMapper.insertUser` SQL 加入 `role` 字段

### 下一步计划
- 创建 `AdminInterceptor` 管理员拦截器
- 配置 `WebConfig` 拦截 `/admin/**` 路径
- 创建 `AdminUserController`
- 创建 `admin-users.html` 管理页面

## 2026-08-14 学习记录

### 学习内容
1. 拦截器注册机制：`WebConfig` 通过 `InterceptorRegistry` 把 `JwtInterceptor` 和 `AdminInterceptor` 注册到 Spring 中。
2. 管理员接口与普通用户接口的权限划分：管理员功能应该放到 `/admin/**` 路径下，由 `AdminInterceptor` 二次校验角色。
3. `@Options(useGeneratedKeys = true, keyProperty = "id")` 的作用：插入后把数据库自增 ID 回填到对象中。
4. `content.trim()` 的作用：去除字符串两端空白，防止只输入空格就提交。
5. `"ADMIN".equals(role)` 这种写法规避空指针异常。
6. 用户禁用逻辑：`status = 1` 表示启用，`status = 0` 表示禁用，登录时检查状态，被禁用户无法登录。

### 完成的功能
1. 修正 `AdminInterceptor` 文件位置，使其包路径与文件路径一致。
2. 将 `UserController` 中的管理员接口（列表、查询、删除、搜索、更新）移动到 `AdminUserController`，避免普通用户越权访问。
3. 完成前端管理页面 `admin-users.html`，支持用户列表分页、搜索、编辑、删除，并在其他页面显示“用户管理”入口（仅管理员可见）。
4. 实现用户启用/禁用功能：
   - 数据库新增 `status` 字段
   - `User` 实体、`UserMapper`、`UserService`、`UserServiceImpl`、`AdminUserController` 均完成改造
   - 前端管理页面支持点击按钮启用/禁用用户
5. 实现帖子评论功能后端：
   - 创建 `comment` 表
   - 新增 `Comment` 实体、`CommentMapper`、`CommentService`、`CommentServiceImpl`、`CommentController`
   - 发表评论、分页查询评论、删除评论（仅评论作者或管理员）
   - `WebConfig` 放行 `/comment/list` 公开接口

### 注意事项
1. `UserController` 中注销账号路径 `/deleteMyccount` 存在拼写错误，已手动修正为 `/deleteMyAccount`。
2. 后端代码应在修改前确认是否允许助手直接修改；本次后续功能已按“先讲解、再操作”的方式进行。
3. `mvn` 命令在当前环境不可用，项目编译与启动需要在 IDEA 中完成。

### 下一步可选方向
1. 前端帖子详情页展示评论列表并发表/删除评论
2. 管理员重置用户密码
3. 管理员删除帖子
4. 用户头像上传


## 2026-08-15 学习记录

### 学习内容
1. 拦截器与请求方法匹配：前端 `fetch` 的 `method` 必须与后端 `@RequestMapping` / `@PostMapping` / `@DeleteMapping` 等注解一致，否则会报 405 错误。
2. 静态资源映射：`WebConfig.addResourceHandlers` 用于把浏览器访问的 URL 路径映射到磁盘上的实际文件夹。
3. 文件上传：前端使用 `FormData` 包装 `MultipartFile`，后端用 `MultipartFile` 接收并保存到磁盘。
4. JWT payload 字段：本项目 token 中用户名存放在 `username` 字段，前端解析时应取 `json.username`。
5. `@RequestHeader` 与 `@RequestParam` 区别：token 放在请求头里传输，后端必须用 `@RequestHeader` 接收。

### 完成的功能
1. 管理员重置用户密码
   - `UserMapper.updatePasswordById`
   - `UserService` / `UserServiceImpl.resetUserPassword`
   - `AdminUserController.POST /admin/user/resetPassword`
   - 前端 `admin-users.html` 增加「重置密码」按钮

2. 管理员删除帖子
   - `PostService` / `PostServiceImpl.adminDeletePost`
   - 新建 `AdminPostController`
   - 路径 `/admin/post/list`、`/admin/post/delete`
   - 新建前端 `admin-posts.html`
   - 各页面导航增加「帖子管理」入口

3. 前端帖子详情页 + 评论功能
   - 新建 `posts.html` 帖子广场（公开分页列表）
   - 新建 `post-detail.html` 帖子详情页
   - 展示帖子内容、评论列表
   - 支持发表评论、删除评论（仅作者或管理员）
   - 各页面帖子标题链接到详情页

4. 用户头像上传
   - 数据库 `user` 表新增 `avatar` 字段
   - `User` 实体类新增 `avatar` 属性
   - `UserMapper.insertUser` SQL 加入 `avatar` 字段
   - `UserMapper.updateAvatar`
   - `UserService` / `UserServiceImpl.updateAvatar`
   - `UserController.POST /user/updateAvatar`
   - `WebConfig` 配置 `/uploads/**` 资源映射
   - 新建 `profile.html` 个人中心页面
   - 各页面导航增加「个人中心」入口

### Bug 修复
1. `PostController.myList` 中 `@RequestParam("Authorization")` 改为 `@RequestHeader("Authorization")`，修复「我的帖子」无法获取当前登录用户的问题。
2. `UserController.updateAvatar` 中头像路径拼接缺少 `/`，由 `/uploads` + 文件名 修正为 `/uploads/` + 文件名。

### 新学/复习知识
1. `URLSearchParams` 解析 URL 参数，例如 `?id=123`。
2. `FormData` 文件上传，不能手动设置 `Content-Type`。
3. `FileReader.readAsDataURL` 实现图片本地预览。
4. `MultipartFile.transferTo()` 保存上传文件。
5. `UUID.randomUUID()` 生成唯一文件名，防止文件覆盖。
6. `addResourceHandler` + `addResourceLocations` 映射静态资源访问路径。

### 下一步可选方向
1. 帖子详情页支持作者编辑/删除自己的帖子
2. 个人中心支持修改用户名和邮箱
3. 帖子搜索功能
4. 评论分页展示
5. 引入 Redis 缓存用户会话或热点数据（建议基础扎实后再学习）


## 2026-08-16 学习记录

### 学习内容
1. Redis 缓存基础
   - Redis 是内存数据库，读写速度快，适合缓存热点数据
   - 缓存的基本流程：读数据先查 Redis，没有再查数据库并写入 Redis
   - 写数据后删除缓存，保证数据一致性
   - 缓存过期时间设置，防止数据永久占用内存
2. Spring Boot 集成 Redis
   - `pom.xml` 添加 `spring-boot-starter-data-redis` 依赖
   - `application.properties` 配置 Redis 连接信息
   - 封装 `RedisUtil` 工具类，支持字符串和对象的存取
3. Redis 缓存帖子详情
   - `PostServiceImpl.getPostById` 先查 Redis，没有再查数据库
   - 点赞/取消点赞后删除帖子详情缓存，保证点赞数实时
4. Redis 缓存用户信息
   - `UserServiceImpl.findByUsername` 先查 Redis，没有再查数据库
   - 修改用户信息后删除旧缓存
5. 缓存一致性问题
   - 缓存中的数据可能不是最新的
   - 写操作（点赞、修改资料）后主动删除缓存，下次读取时重新加载

### 完成的功能
1. 帖子点赞功能
   - 数据库新增 `post_like` 表
   - 新增 `PostLike` 实体、`PostLikeMapper`、`PostLikeService`、`PostLikeServiceImpl`、`PostLikeController`
   - 支持点赞、取消点赞、查询点赞数、判断当前用户是否点赞
   - 前端帖子广场、我的帖子、帖子详情页显示点赞数
   - 帖子详情页支持点赞/取消点赞按钮
2. 评论回复功能
   - 数据库 `comment` 表新增 `parent_id` 字段
   - `Comment` 实体、`CommentMapper`、`CommentService`、`CommentServiceImpl`、`CommentController` 改造支持 `parentId`
   - 前端帖子详情页评论按树形展示（一级评论 + 回复）
   - 每条一级评论支持回复
3. Redis 缓存落地
   - 缓存帖子详情
   - 缓存用户信息
   - 写操作后主动清理缓存

### 新学/复习知识
1. `StringRedisTemplate` 是 Spring 操作 Redis 的入口
2. `ObjectMapper` 用于 Java 对象和 JSON 字符串互转
3. Redis key 命名规范，例如 `post:detail:1`、`user:info:zhangsan`
4. `TimeUnit` 设置缓存过期时间单位
5. 缓存三大问题：缓存穿透、缓存击穿、缓存雪崩（待深入学习）
6. `parent_id` 实现评论回复的树形关系

### 下一步可选方向
1. 消息通知功能（有人点赞/评论/回复时通知作者）
2. 用户关注/粉丝功能
3. 帖子收藏功能
4. 管理员置顶/精华帖
5. 缓存穿透、击穿、雪崩的解决方案
6. 加 Knife4j 接口文档

## 2026-08-17 学习记录

### 学习内容
1. Knife4j 接口文档集成
   - `pom.xml` 添加 `knife4j-openapi3-spring-boot-starter` 依赖
   - 创建 `Knife4jConfig` 配置类，设置文档标题、版本、描述、联系人
   - `WebConfig` 放行 Knife4j 相关路径（`/doc.html`、`/webjars/**`、`/v3/api-docs/**` 等）
   - 访问地址：`http://localhost:8080/doc.html`
   - 接口文档注解：`@Tag`（Controller 分类）、`@Operation`（接口说明）
2. 帖子收藏功能后端实现
   - 数据库建表 `post_favorite`，包含 `id`、`user_id`、`post_id`、`create_time`
   - 使用唯一索引 `uk_user_post` 防止同一用户重复收藏同一帖子
   - 创建 `PostFavorite` 实体类、`PostFavoriteMapper`、`PostFavoriteService`、`PostFavoriteServiceImpl`、`PostFavoriteController`
   - 实现收藏、取消收藏、判断是否收藏、我的收藏列表（分页）四个接口
3. 框架知识复习与深入
   - `pom.xml` 是 Maven 的配置文件，负责依赖管理和项目构建
   - `@Configuration` + `@Bean` 是 Spring 配置类的基础用法
   - `@Component`、`@Service`、`@Controller`、`@Repository` 都是把类交给 Spring 管理
   - `@Mapper` 是 MyBatis 的注解，MyBatis 会生成代理对象并交给 Spring
   - `WebConfig` 是拦截器的"调度员"，决定拦截器何时执行
   - `AdminInterceptor` 是管理员权限拦截器，只拦截 `/admin/**` 路径
   - 异常抛出后会沿调用栈上传，最终被 `GlobalExceptionHandler` 统一处理
   - Controller 层尽量简洁，业务校验可以下沉到 Service 层

### 完成的功能
1. 集成 Knife4j 接口文档，所有 Controller 添加 `@Tag` 和 `@Operation` 中文注解
2. 帖子收藏功能后端接口完成
   - `POST /favorite/add`：收藏帖子
   - `POST /favorite/cancel`：取消收藏
   - `GET /favorite/isFavorite`：判断是否已收藏
   - `GET /favorite/myList`：我的收藏列表（分页）

### 新学/复习知识
1. `knife4j-openapi3-spring-boot-starter` 是 Knife4j 4.x 版本，基于 `springdoc-openapi`
2. `@RestControllerAdvice` + `@ExceptionHandler` 实现全局异常处理
3. 链式调用中，只有最后一行才写分号
4. `PageHelper.startPage(pageNum, pageSize)` 开启分页，`PageInfo<>` 包装分页结果
5. 唯一索引 `UNIQUE KEY` 可以防止重复数据
6. Service 层抛出的 `RuntimeException` 会被 `GlobalExceptionHandler` 捕获并统一返回

### 注意事项
1. Knife4j 的 `/doc.html`、静态资源、API JSON 路径都需要在 `WebConfig` 中放行，否则会被 JWT 拦截器拦截
2. `@Tag` 和 `@Operation` 要使用 `io.swagger.v3.oas.annotations` 包，不要用旧版 springfox 的包
3. `PostFavoriteServiceImpl.listMyFavorites` 存在 N+1 查询问题，后续可优化为 JOIN 查询
4. `PostLikeController` 中存在 `@RequestHeader("Authorizaton")` 拼写错误（正确为 `Authorization`），后续需修复

### 下一步计划
1. 帖子收藏功能前端页面
   - 帖子列表/详情页添加收藏按钮
   - 个人中心添加"我的收藏"入口
2. 修复 `PostLikeController` 中 `Authorization` 拼写错误
3. 继续学习用户关注/粉丝功能或消息通知功能


## 2026-08-19 学习记录

### 学习内容
1. 消息通知功能后端完整实现
   - 数据库 `notification` 表设计：`user_id`、`type`、`content`、`is_read`、`create_time`
   - `NotificationMapper` 新增：`insert`（发送通知）、`markRead`（标记已读）、`countUnread`（统计未读）
   - `NotificationService` / `NotificationServiceImpl` 新增：`sendNotification`、`markRead`、`countUnread`
   - `NotificationController` 新增：`POST /notification/markRead`、`GET /notification/unreadCount`
   - 在 `PostLikeServiceImpl.like` 中点赞成功后给帖子作者发送 `LIKE` 通知
   - 在 `CommentServiceImpl.addComment` 中评论/回复成功后给帖子作者和被回复人发送通知
2. `parent_id` 字段的含义
   - `parent_id == null`：一级评论，直接评论帖子
   - `parent_id != null`：回复某条评论
3. `trim().isEmpty()` 的作用：去掉字符串两端空格后再判断是否为空，防止只输入空格就被当成有效内容
4. `create_time` 默认值：数据库字段设置 `DEFAULT CURRENT_TIMESTAMP` 后，插入时可以不传时间
5. `return false` 与 `throw new RuntimeException()` 的区别
   - `return false`：简单，但前端不知道具体失败原因
   - `throw`：错误信息更明确，会被 `GlobalExceptionHandler` 统一处理
6. 防御性编程：业务代码中查询帖子是否为空，防止空指针异常

### 完成的功能
1. 消息通知后端接口全部完成
   - `GET /notification/myList`：我的通知列表（分页）
   - `POST /notification/markRead?id=xxx`：标记通知已读
   - `GET /notification/unreadCount`：未读通知数
2. 点赞、评论、回复后自动发送通知
3. 修复 `PostLikeController` 拼写问题（代码中已无 `Authorizaton`）
4. 修复 `NotificationController.markRead` 缺失 `@PostMapping` 注解的问题
5. 修复 `CommentServiceImpl.addComment` 中评论插入失败判断 `result < 0` 为 `result <= 0`
6. 修复回复通知中误用 `parentComment.getId()` 为 `parentComment.getUserId()` 的 bug
7. 修复回复通知被错误嵌套在帖子作者通知块中的 bug，使帖子作者回复他人评论时，被回复人也能收到通知

### 新学/复习知识
1. `COUNT(*)` 用于统计满足条件的记录数
2. `@Update` 注解执行 SQL 更新语句
3. `@Param` 注解在 MyBatis 中用于指定参数名
4. 通知类型设计：`LIKE`（点赞）、`COMMENT`（评论）、`REPLY`（回复）
5. 内容截断处理：通知内容超过 200 字符时截取，防止超出数据库字段长度
6. 自己操作自己的内容时不发送通知，避免刷通知（如自己点赞自己帖子、自己回复自己评论）

### 注意事项
1. `NotificationController.markRead` 必须加 `@PostMapping("/markRead")`，否则 Spring 不会把它识别为接口
2. 回复评论时，既要通知帖子作者，也要通知被回复的人，两个逻辑要独立，不能嵌套
3. 比较用户 ID 时要用 `getUserId()`，不要用 `getId()`（`getId()` 是评论 ID）
4. `insertComment` 返回值是成功影响的行数，成功为 1，失败为 0，不会出现负数

### 下一步可选方向
1. 前端消息通知页面
   - 顶部导航显示未读消息数小红点
   - 消息列表页面展示通知
   - 点击消息标记为已读
2. 通知内容优化：显示发送人名字、跳转链接
3. 消息已读全部标记功能
4. 通知按类型筛选（点赞/评论/回复）
