# 学习记录

## 项目基本信息

- 项目名称：login-demo
- 技术栈：Spring Boot + MyBatis + MySQL + JWT
- 项目路径：`login-demo/`
- JDK 版本：17
- Spring Boot 版本：2.6.13

## 已掌握的内容

### 1. 项目结构理解

| 包名 | 作用 |
|------|------|
| `entity` | 数据库表对应的 Java 类 |
| `mapper` | 写 SQL 的地方 |
| `service` | 业务逻辑层 |
| `controller` | 接收前端请求、返回结果 |
| `util/config/common` | 工具类、配置、统一返回格式 |

### 2. 前端 HTML 调用后端的两种方式

- **传统表单提交**：`register.html` 使用 `<form action="/user/register" method="post">`
- **fetch 请求**：`login.html`、`post.html`、`community.html` 使用 JavaScript 的 `fetch`

### 3. JWT 登录认证流程

1. 登录成功后，后端返回 token
2. 前端把 token 存到 `localStorage`
3. 后续请求需要登录的接口时，在请求头里带上 `Authorization: token`
4. 后端 `JwtInterceptor` 验证 token

## 今天新增/学习的后端功能

### 1. 删除用户

- `UserMapper`：`@Delete("DELETE FROM user WHERE id=#{id}")`
- `UserService` / `UserServiceImpl`：`deleteUserById(Long id)`
- `UserController`：`DELETE /user/{id}`

### 2. 根据用户名模糊查询用户

- `UserMapper`：`@Select("SELECT * FROM user WHERE username LIKE CONCAT('%',#{username},'%')")`
- `UserService` / `UserServiceImpl`：`searchUsersByUsername(String username)`
- `UserController`：`GET /user/search?username=xxx`

### 3. 更新用户信息（根据 ID）

- `UserMapper`：`@Update("UPDATE user SET username=#{username},email=#{email} WHERE ID=#{id}")`
- `UserService` / `UserServiceImpl`：`updateUserInfo(Long id, String username, String email)`
- `UserController`：`POST /user/update?id=xxx&username=xxx&email=xxx`
- 注意：这是管理端功能，因为可以传任意 id 改任意用户

### 4. 当前登录用户修改自己的信息

- `UserService` / `UserServiceImpl`：`updateCurrentUserInfo(String currentUsername, String newUsername, String newEmail)`
- `UserController`：`POST /user/updateMyInfo?username=xxx&email=xxx`
- 从 token 里取当前用户名，只能改自己的信息

## 需要注意的问题

1. `UserMapper.java` 里 `updatePassword` 方法的 SQL 有语法错误：`#{username` 缺少右括号 `}`，需要修复。
2. `UserServiceImpl` 中的方法如果实现了接口方法，建议加上 `@Override` 注解。
3. `UserService` 接口中需要声明所有 `UserServiceImpl` 要实现的方法，否则 IDEA 会标红。

## 已理解的概念

- Controller → Service → Mapper → 数据库 三层架构
- `@RequestParam`、`@PathVariable`、`@RequestHeader` 的区别
- `@Param` 注解的作用
- `fetch` 请求的基本用法
- `localStorage` 存取 token
- JWT token 的传递和验证
- SQL 的 `LIKE` 模糊查询
- MyBatis 注解：`@Select`、`@Insert`、`@Update`、`@Delete`

## 下一步可选方向

### 用户端功能
- 用户删除自己的账号
- 用户查看自己发的帖子
- 用户退出登录（前端清 token）
- 用户修改密码（已有，但可复习）

### 管理端功能
- 启用/禁用用户（需要加 `status` 字段）
- 管理员查看所有用户
- 管理员删除任意用户

### 项目优化
- 修复 `updatePassword` 的 SQL 语法错误
- 给 `UserServiceImpl` 的方法统一加 `@Override`
- 添加 `findById` 方法统一查询风格
- 学习全局异常处理 `GlobalExceptionHandler`

## 2026-08-13 学习记录

### 修复与优化
1. 修复 `UserMapper.updatePassword` SQL 语法错误（补全 `}`）
2. 给 `UserServiceImpl.deleteUserById` 添加 `@Override` 注解
3. 修复 `UserController` 中 `delete`、`search`、`update`、`updateMyInfo` 缺失或错误的映射注解

### 新增后端功能
1. 根据 ID 查询用户
   - `UserMapper.findById`
   - `UserService.findById`
   - `UserController.GET /user/{id}`

2. 当前登录用户查看自己发的帖子
   - `PostMapper.findByUserId`
   - `PostService.listByUserId`
   - `PostController.GET /post/myList`

3. 当前登录用户删除自己的账号
   - `UserService.deleteCurrentUser`
   - `UserController.POST /user/deleteMyAccount`

### 新增前端功能
1. `post.html` / `community.html`：添加退出登录按钮
2. 新建 `my-posts.html`：展示当前登录用户的帖子列表
3. `my-posts.html`：添加注销账号按钮和确认逻辑
4. `post.html` / `community.html`：添加“我的帖子”导航链接

### 新学知识
1. 全局异常处理 `GlobalExceptionHandler`
   - `@RestControllerAdvice`：全局捕获 Controller 异常
   - `@ExceptionHandler`：指定处理某种异常
   - `@Slf4j`：Lombok 日志注解
2. `PageHelper` 分页原理
   - `PageHelper.startPage(pageNum, pageSize)` 自动加 `LIMIT`
   - `PageInfo<>(list)` 包装分页结果
3. HTTP 请求基础
   - `Content-Type` 的作用
   - 表单提交与 JSON 提交的区别

### 进行中：管理员功能
1. 用户表添加 `role` 字段（`DEFAULT 'USER'`）
2. 将 `admin` 用户 role 设为 `ADMIN`
3. `User` 实体类添加 `role` 属性
4. `JwtUtil` 在 token 中携带 `role`，并添加 `getRole()` 方法
5. 注册新用户时默认 `role = "USER"`
6. `UserMapper.insertUser` SQL 加入 `role` 字段

### 下一步计划
- 创建 `AdminInterceptor` 管理员拦截器
- 配置 `WebConfig` 拦截 `/admin/**` 路径
- 创建 `AdminUserController`
- 创建 `admin-users.html` 管理页面

## 2026-08-14 学习记录

### 学习内容
1. 拦截器注册机制：`WebConfig` 通过 `InterceptorRegistry` 把 `JwtInterceptor` 和 `AdminInterceptor` 注册到 Spring 中。
2. 管理员接口与普通用户接口的权限划分：管理员功能应该放到 `/admin/**` 路径下，由 `AdminInterceptor` 二次校验角色。
3. `@Options(useGeneratedKeys = true, keyProperty = "id")` 的作用：插入后把数据库自增 ID 回填到对象中。
4. `content.trim()` 的作用：去除字符串两端空白，防止只输入空格就提交。
5. `"ADMIN".equals(role)` 这种写法规避空指针异常。
6. 用户禁用逻辑：`status = 1` 表示启用，`status = 0` 表示禁用，登录时检查状态，被禁用户无法登录。

### 完成的功能
1. 修正 `AdminInterceptor` 文件位置，使其包路径与文件路径一致。
2. 将 `UserController` 中的管理员接口（列表、查询、删除、搜索、更新）移动到 `AdminUserController`，避免普通用户越权访问。
3. 完成前端管理页面 `admin-users.html`，支持用户列表分页、搜索、编辑、删除，并在其他页面显示“用户管理”入口（仅管理员可见）。
4. 实现用户启用/禁用功能：
   - 数据库新增 `status` 字段
   - `User` 实体、`UserMapper`、`UserService`、`UserServiceImpl`、`AdminUserController` 均完成改造
   - 前端管理页面支持点击按钮启用/禁用用户
5. 实现帖子评论功能后端：
   - 创建 `comment` 表
   - 新增 `Comment` 实体、`CommentMapper`、`CommentService`、`CommentServiceImpl`、`CommentController`
   - 发表评论、分页查询评论、删除评论（仅评论作者或管理员）
   - `WebConfig` 放行 `/comment/list` 公开接口

### 注意事项
1. `UserController` 中注销账号路径 `/deleteMyccount` 存在拼写错误，已手动修正为 `/deleteMyAccount`。
2. 后端代码应在修改前确认是否允许助手直接修改；本次后续功能已按“先讲解、再操作”的方式进行。
3. `mvn` 命令在当前环境不可用，项目编译与启动需要在 IDEA 中完成。

### 下一步可选方向
1. 前端帖子详情页展示评论列表并发表/删除评论
2. 管理员重置用户密码
3. 管理员删除帖子
4. 用户头像上传


## 2026-08-15 学习记录

### 学习内容
1. 拦截器与请求方法匹配：前端 `fetch` 的 `method` 必须与后端 `@RequestMapping` / `@PostMapping` / `@DeleteMapping` 等注解一致，否则会报 405 错误。
2. 静态资源映射：`WebConfig.addResourceHandlers` 用于把浏览器访问的 URL 路径映射到磁盘上的实际文件夹。
3. 文件上传：前端使用 `FormData` 包装 `MultipartFile`，后端用 `MultipartFile` 接收并保存到磁盘。
4. JWT payload 字段：本项目 token 中用户名存放在 `username` 字段，前端解析时应取 `json.username`。
5. `@RequestHeader` 与 `@RequestParam` 区别：token 放在请求头里传输，后端必须用 `@RequestHeader` 接收。

### 完成的功能
1. 管理员重置用户密码
   - `UserMapper.updatePasswordById`
   - `UserService` / `UserServiceImpl.resetUserPassword`
   - `AdminUserController.POST /admin/user/resetPassword`
   - 前端 `admin-users.html` 增加「重置密码」按钮

2. 管理员删除帖子
   - `PostService` / `PostServiceImpl.adminDeletePost`
   - 新建 `AdminPostController`
   - 路径 `/admin/post/list`、`/admin/post/delete`
   - 新建前端 `admin-posts.html`
   - 各页面导航增加「帖子管理」入口

3. 前端帖子详情页 + 评论功能
   - 新建 `posts.html` 帖子广场（公开分页列表）
   - 新建 `post-detail.html` 帖子详情页
   - 展示帖子内容、评论列表
   - 支持发表评论、删除评论（仅作者或管理员）
   - 各页面帖子标题链接到详情页

4. 用户头像上传
   - 数据库 `user` 表新增 `avatar` 字段
   - `User` 实体类新增 `avatar` 属性
   - `UserMapper.insertUser` SQL 加入 `avatar` 字段
   - `UserMapper.updateAvatar`
   - `UserService` / `UserServiceImpl.updateAvatar`
   - `UserController.POST /user/updateAvatar`
   - `WebConfig` 配置 `/uploads/**` 资源映射
   - 新建 `profile.html` 个人中心页面
   - 各页面导航增加「个人中心」入口

### Bug 修复
1. `PostController.myList` 中 `@RequestParam("Authorization")` 改为 `@RequestHeader("Authorization")`，修复「我的帖子」无法获取当前登录用户的问题。
2. `UserController.updateAvatar` 中头像路径拼接缺少 `/`，由 `/uploads` + 文件名 修正为 `/uploads/` + 文件名。

### 新学/复习知识
1. `URLSearchParams` 解析 URL 参数，例如 `?id=123`。
2. `FormData` 文件上传，不能手动设置 `Content-Type`。
3. `FileReader.readAsDataURL` 实现图片本地预览。
4. `MultipartFile.transferTo()` 保存上传文件。
5. `UUID.randomUUID()` 生成唯一文件名，防止文件覆盖。
6. `addResourceHandler` + `addResourceLocations` 映射静态资源访问路径。

### 下一步可选方向
1. 帖子详情页支持作者编辑/删除自己的帖子
2. 个人中心支持修改用户名和邮箱
3. 帖子搜索功能
4. 评论分页展示
5. 引入 Redis 缓存用户会话或热点数据（建议基础扎实后再学习）


## 2026-08-16 学习记录

### 学习内容
1. Redis 缓存基础
   - Redis 是内存数据库，读写速度快，适合缓存热点数据
   - 缓存的基本流程：读数据先查 Redis，没有再查数据库并写入 Redis
   - 写数据后删除缓存，保证数据一致性
   - 缓存过期时间设置，防止数据永久占用内存
2. Spring Boot 集成 Redis
   - `pom.xml` 添加 `spring-boot-starter-data-redis` 依赖
   - `application.properties` 配置 Redis 连接信息
   - 封装 `RedisUtil` 工具类，支持字符串和对象的存取
3. Redis 缓存帖子详情
   - `PostServiceImpl.getPostById` 先查 Redis，没有再查数据库
   - 点赞/取消点赞后删除帖子详情缓存，保证点赞数实时
4. Redis 缓存用户信息
   - `UserServiceImpl.findByUsername` 先查 Redis，没有再查数据库
   - 修改用户信息后删除旧缓存
5. 缓存一致性问题
   - 缓存中的数据可能不是最新的
   - 写操作（点赞、修改资料）后主动删除缓存，下次读取时重新加载

### 完成的功能
1. 帖子点赞功能
   - 数据库新增 `post_like` 表
   - 新增 `PostLike` 实体、`PostLikeMapper`、`PostLikeService`、`PostLikeServiceImpl`、`PostLikeController`
   - 支持点赞、取消点赞、查询点赞数、判断当前用户是否点赞
   - 前端帖子广场、我的帖子、帖子详情页显示点赞数
   - 帖子详情页支持点赞/取消点赞按钮
2. 评论回复功能
   - 数据库 `comment` 表新增 `parent_id` 字段
   - `Comment` 实体、`CommentMapper`、`CommentService`、`CommentServiceImpl`、`CommentController` 改造支持 `parentId`
   - 前端帖子详情页评论按树形展示（一级评论 + 回复）
   - 每条一级评论支持回复
3. Redis 缓存落地
   - 缓存帖子详情
   - 缓存用户信息
   - 写操作后主动清理缓存

### 新学/复习知识
1. `StringRedisTemplate` 是 Spring 操作 Redis 的入口
2. `ObjectMapper` 用于 Java 对象和 JSON 字符串互转
3. Redis key 命名规范，例如 `post:detail:1`、`user:info:zhangsan`
4. `TimeUnit` 设置缓存过期时间单位
5. 缓存三大问题：缓存穿透、缓存击穿、缓存雪崩（待深入学习）
6. `parent_id` 实现评论回复的树形关系

### 下一步可选方向
1. 消息通知功能（有人点赞/评论/回复时通知作者）
2. 用户关注/粉丝功能
3. 帖子收藏功能
4. 管理员置顶/精华帖
5. 缓存穿透、击穿、雪崩的解决方案
6. 加 Knife4j 接口文档

## 2026-08-17 学习记录

### 学习内容
1. Knife4j 接口文档集成
   - `pom.xml` 添加 `knife4j-openapi3-spring-boot-starter` 依赖
   - 创建 `Knife4jConfig` 配置类，设置文档标题、版本、描述、联系人
   - `WebConfig` 放行 Knife4j 相关路径（`/doc.html`、`/webjars/**`、`/v3/api-docs/**` 等）
   - 访问地址：`http://localhost:8080/doc.html`
   - 接口文档注解：`@Tag`（Controller 分类）、`@Operation`（接口说明）
2. 帖子收藏功能后端实现
   - 数据库建表 `post_favorite`，包含 `id`、`user_id`、`post_id`、`create_time`
   - 使用唯一索引 `uk_user_post` 防止同一用户重复收藏同一帖子
   - 创建 `PostFavorite` 实体类、`PostFavoriteMapper`、`PostFavoriteService`、`PostFavoriteServiceImpl`、`PostFavoriteController`
   - 实现收藏、取消收藏、判断是否收藏、我的收藏列表（分页）四个接口
3. 框架知识复习与深入
   - `pom.xml` 是 Maven 的配置文件，负责依赖管理和项目构建
   - `@Configuration` + `@Bean` 是 Spring 配置类的基础用法
   - `@Component`、`@Service`、`@Controller`、`@Repository` 都是把类交给 Spring 管理
   - `@Mapper` 是 MyBatis 的注解，MyBatis 会生成代理对象并交给 Spring
   - `WebConfig` 是拦截器的"调度员"，决定拦截器何时执行
   - `AdminInterceptor` 是管理员权限拦截器，只拦截 `/admin/**` 路径
   - 异常抛出后会沿调用栈上传，最终被 `GlobalExceptionHandler` 统一处理
   - Controller 层尽量简洁，业务校验可以下沉到 Service 层

### 完成的功能
1. 集成 Knife4j 接口文档，所有 Controller 添加 `@Tag` 和 `@Operation` 中文注解
2. 帖子收藏功能后端接口完成
   - `POST /favorite/add`：收藏帖子
   - `POST /favorite/cancel`：取消收藏
   - `GET /favorite/isFavorite`：判断是否已收藏
   - `GET /favorite/myList`：我的收藏列表（分页）

### 新学/复习知识
1. `knife4j-openapi3-spring-boot-starter` 是 Knife4j 4.x 版本，基于 `springdoc-openapi`
2. `@RestControllerAdvice` + `@ExceptionHandler` 实现全局异常处理
3. 链式调用中，只有最后一行才写分号
4. `PageHelper.startPage(pageNum, pageSize)` 开启分页，`PageInfo<>` 包装分页结果
5. 唯一索引 `UNIQUE KEY` 可以防止重复数据
6. Service 层抛出的 `RuntimeException` 会被 `GlobalExceptionHandler` 捕获并统一返回

### 注意事项
1. Knife4j 的 `/doc.html`、静态资源、API JSON 路径都需要在 `WebConfig` 中放行，否则会被 JWT 拦截器拦截
2. `@Tag` 和 `@Operation` 要使用 `io.swagger.v3.oas.annotations` 包，不要用旧版 springfox 的包
3. `PostFavoriteServiceImpl.listMyFavorites` 存在 N+1 查询问题，后续可优化为 JOIN 查询
4. `PostLikeController` 中存在 `@RequestHeader("Authorizaton")` 拼写错误（正确为 `Authorization`），后续需修复

### 下一步计划
1. 帖子收藏功能前端页面
   - 帖子列表/详情页添加收藏按钮
   - 个人中心添加"我的收藏"入口
2. 修复 `PostLikeController` 中 `Authorization` 拼写错误
3. 继续学习用户关注/粉丝功能或消息通知功能


## 2026-08-19 学习记录

### 学习内容
1. 消息通知功能后端完整实现
   - 数据库 `notification` 表设计：`user_id`、`type`、`content`、`is_read`、`create_time`
   - `NotificationMapper` 新增：`insert`（发送通知）、`markRead`（标记已读）、`countUnread`（统计未读）
   - `NotificationService` / `NotificationServiceImpl` 新增：`sendNotification`、`markRead`、`countUnread`
   - `NotificationController` 新增：`POST /notification/markRead`、`GET /notification/unreadCount`
   - 在 `PostLikeServiceImpl.like` 中点赞成功后给帖子作者发送 `LIKE` 通知
   - 在 `CommentServiceImpl.addComment` 中评论/回复成功后给帖子作者和被回复人发送通知
2. `parent_id` 字段的含义
   - `parent_id == null`：一级评论，直接评论帖子
   - `parent_id != null`：回复某条评论
3. `trim().isEmpty()` 的作用：去掉字符串两端空格后再判断是否为空，防止只输入空格就被当成有效内容
4. `create_time` 默认值：数据库字段设置 `DEFAULT CURRENT_TIMESTAMP` 后，插入时可以不传时间
5. `return false` 与 `throw new RuntimeException()` 的区别
   - `return false`：简单，但前端不知道具体失败原因
   - `throw`：错误信息更明确，会被 `GlobalExceptionHandler` 统一处理
6. 防御性编程：业务代码中查询帖子是否为空，防止空指针异常

### 完成的功能
1. 消息通知后端接口全部完成
   - `GET /notification/myList`：我的通知列表（分页）
   - `POST /notification/markRead?id=xxx`：标记通知已读
   - `GET /notification/unreadCount`：未读通知数
2. 点赞、评论、回复后自动发送通知
3. 修复 `PostLikeController` 拼写问题（代码中已无 `Authorizaton`）
4. 修复 `NotificationController.markRead` 缺失 `@PostMapping` 注解的问题
5. 修复 `CommentServiceImpl.addComment` 中评论插入失败判断 `result < 0` 为 `result <= 0`
6. 修复回复通知中误用 `parentComment.getId()` 为 `parentComment.getUserId()` 的 bug
7. 修复回复通知被错误嵌套在帖子作者通知块中的 bug，使帖子作者回复他人评论时，被回复人也能收到通知

### 新学/复习知识
1. `COUNT(*)` 用于统计满足条件的记录数
2. `@Update` 注解执行 SQL 更新语句
3. `@Param` 注解在 MyBatis 中用于指定参数名
4. 通知类型设计：`LIKE`（点赞）、`COMMENT`（评论）、`REPLY`（回复）
5. 内容截断处理：通知内容超过 200 字符时截取，防止超出数据库字段长度
6. 自己操作自己的内容时不发送通知，避免刷通知（如自己点赞自己帖子、自己回复自己评论）

### 注意事项
1. `NotificationController.markRead` 必须加 `@PostMapping("/markRead")`，否则 Spring 不会把它识别为接口
2. 回复评论时，既要通知帖子作者，也要通知被回复的人，两个逻辑要独立，不能嵌套
3. 比较用户 ID 时要用 `getUserId()`，不要用 `getId()`（`getId()` 是评论 ID）
4. `insertComment` 返回值是成功影响的行数，成功为 1，失败为 0，不会出现负数

### 下一步可选方向
1. 前端消息通知页面
   - 顶部导航显示未读消息数小红点
   - 消息列表页面展示通知
   - 点击消息标记为已读
2. 通知内容优化：显示发送人名字、跳转链接
3. 消息已读全部标记功能
4. 通知按类型筛选（点赞/评论/回复）


## 2026-08-23 学习记录

### 学习内容
1. 项目架构分析
   - 全面 review 项目代码，发现主要问题是缺少事务控制、异常处理不统一
   - `post_like`、`post_favorite`、`user_follow` 等表已有联合唯一索引，数据库层面能兜底防重复
   - 当前阶段不建议引入 Redis 分布式锁，先补基础能力性价比更高
2. Spring 声明式事务
   - `@Transactional(rollbackFor = Exception.class)` 的作用
   - 事务保证多步骤业务操作的原子性，任一异常自动回滚
   - 事务是通过 AOP 实现的，`@Transactional` 本质上是一个特殊的 AOP 切面
3. AOP 面向切面编程
   - 把日志、权限、事务等通用逻辑从业务代码中剥离
   - 项目中的 `WebLogAspect` 是自定义 AOP 切面
   - `@Transactional` 是 Spring 内置的 AOP 切面
4. 统一异常处理与 Service 层职责
   - Controller 统一通过 `userService.getCurrentUser(token)` 获取当前用户
   - Service 层业务错误统一抛 `RuntimeException`
   - `GlobalExceptionHandler` 统一捕获异常并返回 `Result.error(...)`
   - Controller 代码更简洁，错误格式统一
5. JWT 密钥配置化
   - 不直接把密钥写死在 `JwtUtil` 中
   - 通过 `application.properties` 配置 `jwt.secret`
   - 用 `JwtConfig` 在启动时注入到 `JwtUtil`
6. Knife4j 问题排查
   - `/v3/api-docs` 正常返回 JSON，说明后端 Swagger 初始化正常
   - 页面能打开但不显示内容，通常是浏览器缓存或插件干扰
   - 无痕模式可正常访问，确认代码无问题

### 完成的功能与优化
1. 添加事务控制
   - 为点赞、取消点赞、关注、取消关注、收藏、取消收藏、评论、发帖等关键写操作添加 `@Transactional(rollbackFor = Exception.class)`
2. 统一用户获取方式
   - `UserService` / `UserServiceImpl` 新增 `getCurrentUser(String token)`
   - 所有 Controller 统一调用该方法，不再各自 `JwtUtil.getUsername(token)` + `findByUsername()`
3. Service 不再接收 token
   - `PostFavoriteService`、`UserFollowService`、`PrivateMessageService`、`NotificationService` 接口及实现方法参数从 `String token` 改为 `Long userId`
   - 删除各 Service 中重复的 `getCurrentUser(token)` 私有方法
4. 业务错误改抛异常
   - 点赞已存在、未点赞、已关注、未关注、评论失败、无权修改/删除帖子等场景改为抛 `RuntimeException`
   - 修改密码旧密码错误、用户不存在等场景改为抛异常
5. Bug 修复
   - 修复 `NotificationMapper.findByUserIdAndType` 中 `tpye` 拼写为 `type`
6. 配置优化
   - `application.properties` 新增 JWT 密钥配置
   - 新增 `JwtConfig` 配置类读取并注入密钥
   - 补全 `WebConfig` 中 Knife4j / Swagger 静态资源放行路径
   - 添加 `spring.mvc.pathmatch.matching-strategy=ant_path_matcher` 兼容 Spring Boot 2.6+
7. Knife4j 版本调整
   - 将 `knife4j-openapi3-spring-boot-starter` 从 `4.4.0` 降级到 `4.3.0`（后续确认无痕模式可正常访问，主要问题为浏览器插件/缓存）

### 新学/复习知识
1. `@Transactional` 可以加在类上或方法上，方法优先于类
2. `rollbackFor = Exception.class` 让所有异常（包括受检异常）都触发回滚
3. 事务针对一次方法调用，不是针对整个类
4. 同类中方法 A 调用方法 B，`@Transactional` 会失效（Spring AOP 自调用问题）
5. Service 层抛异常后，Controller 中该异常之后的代码不会执行，最终由全局异常处理器返回错误
6. Redis 缓存三大问题
   - 缓存穿透：请求不存在的数据，解决方案如缓存 null 值、布隆过滤器
   - 缓存击穿：热点 key 过期瞬间大量请求打到数据库，解决方案如互斥锁、热点 key 永不过期
   - 缓存雪崩：大量 key 同时过期，解决方案如随机过期时间、多级缓存
   - 当前项目用 `synchronized` + 缓存 null + 随机过期时间做了基础防护

### 注意事项
1. 改完大量代码后必须重启项目并在 IDEA 中刷新 Maven
2. 浏览器插件和缓存会导致 Knife4j 页面显示异常，排查时先用无痕模式
3. `favicon.ico` 报 401 不影响功能，但说明 JWT 拦截器对某些静态资源仍需放行
4. 分布式锁虽然值得学，但当前项目阶段更适合先打好基础（事务、异常统一、缓存一致性）

### 下一步可选方向
1. 深入学习 Redisson 分布式锁，替换 `synchronized` 解决多实例缓存击穿
2. 学习布隆过滤器，更优雅地解决缓存穿透
3. MySQL 索引与 Explain 执行计划学习，优化项目查询
4. 优化 `PostFavoriteServiceImpl.listMyFavorites` 的 N+1 查询问题
5. 消息队列（RabbitMQ/RocketMQ）学习，用于异步发送通知
6. Docker 部署：把项目 + MySQL + Redis 一键跑起来


## 2026-08-24 学习记录

### 学习内容

#### 1. Redisson 分布式锁

1. **为什么 `synchronized` 不够**
   - `synchronized` 是 JVM 级别的锁，只能保证同一个 JVM 进程内的线程互斥
   - 生产环境通常多实例部署，每个 JVM 有自己的锁，无法跨实例互斥
   - 热点 key 过期时，多个实例可能同时查数据库，缓存击穿问题仍然存在

2. **分布式锁的作用**
   - 让所有实例、所有线程共享同一把锁
   - 这把锁放在大家都能访问的地方（如 Redis）
   - 同一时间只有一个线程能执行重建缓存等关键代码

3. **Redisson 是什么**
   - 一个 Java 的 Redis 工具库
   - 在 Redis 命令基础上封装了完整的分布式锁功能
   - 解决了锁续期、误释放、可重入等复杂问题

4. **Redisson 与 RedissonClient 的区别**
   - `Redisson`：一个 Java 类，工厂类，负责创建 `RedissonClient`
   - `RedissonClient`：接口/对象，代表 Java 程序与 Redis 的连接，真正操作 Redis
   - `Redisson.create(config)` 创建 `RedissonClient`，之后通过 `redissonClient` 获取锁

5. **RLock 分布式锁**
   - `RLock` 是 Redisson 提供的分布式锁接口
   - `RLock lock = redissonClient.getLock("lock:" + key)`：根据 key 获取锁对象
   - `lock.lock()`：加锁，拿不到就阻塞等待
   - `lock.unlock()`：释放锁
   - 锁的 key 建议加 `lock:` 前缀，与缓存 key 区分开

6. **看门狗机制（Watch Dog）**
   - `lock.lock()` 默认锁 30 秒过期
   - 业务未执行完时，Redisson 会自动续期
   - 默认每 10 秒续期一次，重新设置为 30 秒
   - 业务执行完调用 `unlock()` 后，看门狗停止

7. **`lock()` 与 `tryLock()` 的区别**
   - `lock()`：拿不到锁就一直等待，适合缓存重建等读数据场景
   - `tryLock()`：拿不到锁立即返回 false，适合秒杀、限流等场景
   - `tryLock(3, TimeUnit.SECONDS)`：最多等待 3 秒，拿不到再返回 false

8. **可重入锁**
   - 同一个线程可以多次获取同一把锁，不会死锁
   - Redisson 的 `RLock` 默认可重入
   - 加几次锁就要释放几次，锁才真正释放

### 完成的功能

1. **添加 Redisson 依赖**
   - 在 `pom.xml` 中添加 `redisson-spring-boot-starter` 3.23.5

2. **创建 Redisson 配置类**
   - 新建 `RedissonConfig.java`
   - 从 `application.properties` 读取 Redis 配置
   - 创建 `RedissonClient` Bean 交给 Spring 管理

3. **改造 `PostServiceImpl.getPostById`**
   - 用 `synchronized` 替换为 Redisson `RLock`
   - 先查缓存 → 加分布式锁 → 再查缓存 → 查数据库 → 重建缓存 → 释放锁
   - 数据库不存在时缓存空值，防止缓存穿透

4. **改造 `UserServiceImpl.findByUsername`**
   - 同样加入 Redisson 分布式锁
   - 缓存空值防止缓存穿透

### 新学/复习知识

1. `SETNX` 是 Redis 最原始的加锁命令，但直接使用有诸多坑
2. Redisson 封装了分布式锁的完整实现，生产环境更可靠
3. `try-finally` 保证锁一定会释放，避免死锁
4. 分布式锁适合缓存重建等必须拿到结果的场景，`tryLock` 适合可以失败的场景
5. 缓存穿透可用缓存空值解决，布隆过滤器是更优雅的方案

### 下一步计划

1. 学习布隆过滤器，更优雅地解决缓存穿透
2. 在项目中集成 Redisson 布隆过滤器
3. 改造查询逻辑：先过布隆过滤器，再走缓存/数据库


## 2026-08-25 学习记录

### 学习内容

#### 1. 布隆过滤器（Bloom Filter）基础

1. **核心特性**
   - 布隆过滤器用于判断“一个元素是否可能存在于集合中”
   - 返回 `true` 表示“可能存在”，有一定概率误判（假阳性）
   - 返回 `false` 表示“肯定不存在”，不会漏判
   - 内部由位数组 + 多个哈希函数组成

2. **不能删除单个元素的原因**
   - 多个元素可能通过哈希函数映射到同一个位置
   - 删除一个元素会清理其他元素共享的位置，导致其他元素被误判为不存在
   - 解决方案：定时重建布隆过滤器

3. **适用场景**
   - 数据量大
   - 需要做“是否存在”的快速判断
   - 能接受少量误判
   - 删除不频繁或可通过重建解决

#### 2. Redisson 布隆过滤器

1. **依赖**
   - 项目已引入 `redisson-spring-boot-starter` 3.23.5
   - 通过 `RedissonClient` 获取 `RBloomFilter` 对象

2. **关键 API**
   - `redissonClient.getBloomFilter(name)`：获取布隆过滤器
   - `bloomFilter.tryInit(expectedInsertions, falseProbability)`：初始化
   - `bloomFilter.add(element)`：添加元素
   - `bloomFilter.contains(element)`：判断元素是否可能存在
   - `bloomFilter.delete()`：删除整个布隆过滤器

3. **两个核心参数**
   - `expectedInsertions`：预计存放元素数量
   - `falseProbability`：误判率，越低占用内存越大

#### 3. Spring 定时任务

1. **`@EnableScheduling`**
   - 放在启动类上，开启定时任务功能
   - 是定时任务的总开关

2. **`@Scheduled`**
   - 放在 Bean 方法上，标记该方法定时执行
   - 可用 `cron` 表达式指定执行时间
   - `cron` 格式：`秒 分 时 日 月 周`

3. **常用 cron 示例**
   - `0 0 3 * * ?`：每天凌晨 3 点
   - `0 0/10 * * * ?`：每 10 分钟执行一次
   - `0 0 8 ? * MON-FRI`：每周一到周五早上 8 点

#### 4. 配置外部化

1. **`@Value` 注解**
   - 从 `application.properties` 读取配置值
   - 注入的字段不能是 `static`
   - 配置项 key 和代码中的 `${...}` 必须一致

2. **好处**
   - 改参数不用改代码
   - 不用重新编译
   - 不同环境可配置不同值

---

### 完成的功能

#### 1. 用户名布隆过滤器

1. **新建 `BloomFilterUtil`**
   - Redis key：`username:bloom:filter`
   - 提供 `init`、`add`、`mightContain` 方法

2. **新建 `BloomFilterInitRunner`**
   - 实现 `CommandLineRunner`
   - 项目启动时加载所有用户名并初始化布隆过滤器

3. **改造注册逻辑 `UserServiceImpl.register`**
   - 先用布隆过滤器判断用户名是否可能存在
   - 可能存在时再去数据库确认
   - 注册成功后把新用户名加入布隆过滤器

4. **改造 `UserServiceImpl.findByUsername`**
   - 在查 Redis 之前先过布隆过滤器
   - 肯定不存在的直接返回 null，防止缓存穿透

5. **处理用户名修改场景**
   - 在 `updateCurrentUserInfo` 和 `updateUserInfo` 中，改名后把新用户名加入布隆过滤器

#### 2. 帖子 ID 布隆过滤器

1. **新建 `PostBloomFilterUtil`**
   - Redis key：`post:id:bloom:filter`
   - 存 `Long` 类型帖子 ID

2. **新建 `PostBloomFilterInitRunner`**
   - 项目启动时加载所有帖子 ID 并初始化布隆过滤器

3. **改造 `PostServiceImpl.getPostById`**
   - 在最前面加布隆过滤器判断
   - 肯定不存在的帖子 ID 直接返回 null

4. **改造 `PostServiceImpl.createPost`**
   - 发帖成功后，把新帖子 ID 加入布隆过滤器
   - 依赖 `@Options(useGeneratedKeys = true, keyProperty = "id")` 回填自增 ID

#### 3. 定时重建布隆过滤器

1. **启动类添加 `@EnableScheduling`**
2. **新建 `BloomFilterRebuildJob`**
   - 使用 `@Scheduled(cron = "0 0 3 * * ?")`
   - 每天凌晨 3 点重建用户名和帖子 ID 两个布隆过滤器
   - 解决删除用户/帖子后布隆过滤器残留的问题

#### 4. 布隆过滤器参数配置化

1. **`application.properties` 新增配置**
   - `bloom.filter.username.expected-insertions`
   - `bloom.filter.username.false-probability`
   - `bloom.filter.post.expected-insertions`
   - `bloom.filter.post.false-probability`

2. **改造 `BloomFilterUtil` 和 `PostBloomFilterUtil`**
   - 删除硬编码的 `static final` 常量
   - 使用 `@Value` 从配置文件读取参数

---

### 新学/复习知识

1. 布隆过滤器原理、误判率、不能删除的缺陷
2. `RBloomFilter` 的基本 API
3. `CommandLineRunner` 在项目启动后自动执行
4. `@EnableScheduling` 和 `@Scheduled` 实现定时任务
5. `cron` 表达式格式和常用写法
6. `@Value` 配置外部化
7. 缓存穿透的多种解决方案：缓存空值、布隆过滤器
8. 删除数据后布隆过滤器残留的处理方案：定时重建

---

### 注意事项

1. 布隆过滤器说“可能存在”时，仍需查数据库确认，因为可能误判
2. 布隆过滤器说“肯定不存在”时，可以直接返回，不会漏判
3. 新增数据后要及时加入布隆过滤器，否则会出现“数据存在但过滤器说没有”的 bug
4. 用户名修改后要把新用户名加入布隆过滤器
5. 定时重建会短暂删除旧布隆过滤器，期间判断逻辑可能受影响，但一般凌晨低峰期执行可接受
6. `@Value` 注入的字段不能是 `static`

---

### 下一步可选方向

1. MySQL Explain 执行计划学习，优化项目查询
2. 消息队列（RabbitMQ/RocketMQ）异步发送通知
3. Docker 部署：把项目 + MySQL + Redis 一键跑起来
4. 优化 `PostFavoriteServiceImpl.listMyFavorites` 的 N+1 查询问题
5. 继续深入学习 Redisson 其他功能


## 2026-08-27 学习记录

### 学习内容

#### 1. MySQL EXPLAIN 执行计划

1. **EXPLAIN 作用**
   - 查看 MySQL 打算如何执行一条 SQL
   - 判断 SQL 是走索引还是全表扫描
   - 重点观察列：`type`、`key`、`rows`、`Extra`

2. **关键列含义**
   - `type`：访问类型，从好到差大致为 `system > const > eq_ref > ref > range > index > ALL`
   - `key`：实际使用的索引，`NULL` 表示没走索引
   - `rows`：估算扫描行数，越小越好
   - `Extra`：额外信息，`Using filesort` 表示需要额外排序，`Using where` 表示用 WHERE 过滤

3. **典型执行计划判断**
   - `type = ALL` 且 `key = NULL`：全表扫描，通常需要优化
   - `Extra = Using filesort`：需要额外排序，可通过复合索引优化

#### 2. 索引基础

1. **索引是什么**
   - 类似书的目录，把字段值映射到数据位置
   - 没有索引时只能逐行扫描（全表扫描）

2. **索引类型**
   - 主键索引：`PRIMARY KEY`，唯一 + 非空
   - 唯一索引：`UNIQUE INDEX`，加速查询 + 保证不重复
   - 普通索引：`INDEX`，只加速查询
   - 复合索引：多个字段组合成一个索引

3. **索引优缺点**
   - 优点：加速查询、优化排序
   - 缺点：占用磁盘空间、降低写入速度、增加维护成本

#### 3. 复合索引与最左前缀原则

1. **复合索引设计模式**
   - 常见模式：`(过滤字段, 排序字段)`
   - 例如 `(user_id, create_time DESC)` 可同时优化 `WHERE user_id = ?` 和 `ORDER BY create_time DESC`

2. **最左前缀原则**
   - 复合索引必须从最左列开始用，中间不能跳过
   - 索引 `(a, b, c)`：
     - `WHERE a = 1`：可用 a
     - `WHERE a = 1 AND b = 2`：可用 a, b
     - `WHERE a = 1 AND b = 2 AND c = 3`：可用 a, b, c
     - `WHERE b = 2`：不可用（跳过 a）
     - `WHERE a = 1 AND c = 3`：只可用 a（跳过 b）

3. **复合索引优化 ORDER BY**
   - 如果索引顺序和 `ORDER BY` 一致，可以避免 `Using filesort`
   - 范围查询后，`ORDER BY` 同一索引列仍然可以避免 filesort

#### 4. 索引失效场景

1. `LIKE '%xxx%'` 以 `%` 开头不走普通索引
2. `OR` 两边条件没有同时走索引时可能全表扫描
3. 对索引字段使用函数或运算
4. 违反最左前缀原则
5. 小表可能故意不走索引（优化器认为全表扫描更快）

#### 5. 覆盖索引

1. **概念**
   - 不是特殊的索引类型，而是一种使用场景
   - 查询需要的所有字段都在当前使用的索引里，MySQL 不需要回表
   - `EXPLAIN` 的 `Extra` 会显示 `Using index`

2. **关键条件**
   - 查询字段必须都在索引中（包括主键，因为二级索引叶子节点带主键）
   - 通常需要避免 `SELECT *`，只查需要的字段

#### 6. 索引设计原则

1. 优先为高频、慢查询建索引
2. 一个索引尽量覆盖多个相似查询（利用最左前缀）
3. 避免重复或冗余索引
4. 索引数量要控制，不是越多越好
5. 写操作频繁的表，索引不宜过多

### 完成的工作

#### 1. 分析项目所有 Mapper SQL

- 检查了 `UserMapper`、`PostMapper`、`CommentMapper`、`NotificationMapper`、`PrivateMessageMapper`、`UserFollowMapper`、`PostFavoriteMapper`、`PostLikeMapper`、`CommunityMapper`
- 找出需要优化的查询，主要分为三类：
  - 需要复合索引的列表查询（`WHERE + ORDER BY`）
  - 需要计数索引的 `COUNT(*)` 查询
  - 普通索引无法解决的模糊搜索

#### 2. 为数据库表添加索引

- `post` 表：
  - `idx_community_time (community_id, create_time DESC)`
  - `idx_user_time (user_id, create_time DESC)`

- `comment` 表：
  - `idx_post_time (post_id, create_time DESC)`

- `notification` 表：
  - `idx_user_time (user_id, create_time DESC)`
  - `idx_user_read (user_id, is_read)`
  - `idx_user_type_time (user_id, type, create_time DESC)`

- `private_message` 表：
  - `idx_receiver_time (receiver_id, create_time DESC)`
  - `idx_sender_time (sender_id, create_time DESC)`
  - `idx_receiver_read (receiver_id, is_read)`

- `user_follow` 表：
  - `idx_follower_time (follower_id, create_time DESC)`
  - `idx_following_time (following_id, create_time DESC)`

- `post_favorite` 表：
  - `idx_user_time (user_id, create_time DESC)`

#### 3. 用 EXPLAIN 验证索引效果

- 加索引前：`type = ALL`、`key = NULL`、`Extra = Using where; Using filesort`
- 加索引后：`type = ref`、`key = 对应索引`、`Extra = NULL` 或 `Using where`
- 验证了 `post`、`comment`、`notification`、`private_message`、`user_follow`、`post_favorite` 等表的关键查询

### 新学/复习知识

1. `SHOW INDEX FROM 表名` 查看表的索引
2. `ALTER TABLE 表名 ADD INDEX 索引名 (字段1, 字段2)` 添加索引
3. `ALTER TABLE 表名 DROP INDEX 索引名` 删除索引
4. `key_len` 可以判断 SQL 用到了索引的多少列
5. `Backward index scan`：MySQL 8.0 支持反向扫描索引，避免 filesort
6. `Using index condition`：在存储引擎层用索引条件过滤
7. 索引设计是取舍艺术，需要在读写性能之间平衡

### 注意事项

1. 不要为每一个 SQL 都单独建索引，优先考虑用最左前缀覆盖多个查询
2. `SELECT *` 很难使用覆盖索引，实际开发中应尽量只查需要的字段
3. 索引不是越多越好，过多索引会降低写入性能并占用磁盘空间
4. 小表可能不走索引，数据量大时索引效果才明显
5. `LIKE '%xxx%'` 和 `OR` 组合普通索引救不了，需要考虑全文索引或 Elasticsearch
6. 加索引操作会锁表，生产环境大表加索引需要谨慎，建议在低峰期执行

### 尚未解决的问题

1. `PostMapper.findByKeyword` 的标题/内容模糊搜索仍走全表扫描
2. `UserMapper.findByUsernameLike` 的用户名模糊搜索仍走全表扫描
3. 后续可考虑学习 MySQL 全文索引（FULLTEXT）或 Elasticsearch

### 下一步可选方向

1. 学习 MySQL 全文索引，解决项目中的模糊搜索问题
2. 学习 `EXPLAIN ANALYZE` / `SHOW PROFILE`，看 SQL 真实执行时间
3. 学习 SQL 慢查询日志分析
4. 优化 `PostFavoriteServiceImpl.listMyFavorites` 的 N+1 查询问题
5. 学习消息队列（RabbitMQ/RocketMQ），用于异步发送通知
